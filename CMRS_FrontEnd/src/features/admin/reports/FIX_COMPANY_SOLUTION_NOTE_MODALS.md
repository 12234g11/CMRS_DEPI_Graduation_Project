# Company solution review note flow

- AcceptFix opens a required admin-note modal and sends only its value as `adminNote`.
- RequestCompletion opens a required completion-details modal and sends only its value as `adminNote`.
- The two inline fields are hidden for the fixed-solution review scenario.
- Other company-response scenarios remain unchanged.
